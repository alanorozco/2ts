/** @typedef {Array<Foo>} */
let Simple;

/** @typedef {Array<Array<Bar>>} */
let Nested;

/** @typedef {Array<Promise<Array<Baz>>>} */
let Interlaced;

/** @typedef {Set<Array<Promise<Array<Foo>>>>} */
let Deep;
